# Common Sources

This directory contains files that are byte-for-byte shared by all supported
Minecraft versions.

Keep Forge, NeoForge, mapping, data-path, and dependency-specific code in the
matching `versions/<minecraft-version>` project. Move files here only when the
same path and content can be compiled or loaded by every supported version.
