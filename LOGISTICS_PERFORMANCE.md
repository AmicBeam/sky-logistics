# 物流传输性能机制

本文只描述容器之间的传输路径，不包含天穹物品库等长期存储端点。

## 源容器热槽与健康路径

- 成功提供物品的源槽会进入热槽集合；后续传输优先检查热槽。
- 若本轮优先检查的热槽已经为空，会在剩余工作预算允许时额外顺序检查一个槽位；同一源端点的单轮计划最多触发一次这种兜底，避免整组搬空后连续数 tick 只重试空热槽。触发兜底时会重置冷槽发现的成功次数计数，并跳过本轮额外冷槽探测。
- 新的冷槽发现不会抢占热槽机会。主传输成功后，仅在冷槽探测间隔到达且本次仍有操作预算时，额外探测一个冷槽。
- 每个热槽在同一 tick 最多被调度一次；操作率超过当前已学习热槽数时，剩余次数继续用于顺序发现，不会绕回刚抽空的热槽。运行时缓存容量与当前配置不一致时会原地调整并保留已有热槽，不再要求破坏节点重建。
- 容器、管线或端点结构变化时，对应端点缓存会失效并重新建立；物品数量和槽位内容的高频变化不会主动触发整条线路重建。
- 健康路径不是一对一专用。少量端点的多对多网络也能复用，但每次源端点默认只尝试一个目标端点，避免大量失败目标把单次调度成本放大。

## 线路资源路由与负能力缓存

- 线路建立时会汇总物品、流体、化学品、能量、魔力和源质的输入/输出位图。某类资源没有本地完整路由时，普通端点不会进入该资源的传输与唤醒分支；维度升级仍可使用全局输出。
- 跨维度输出索引按“线路 + 资源类型”在维度升级首次实际访问时惰性创建；从未跨维度传输的线路不维护全局列表。索引创建后会保留到服务器状态清理，并随端点和优先级变化原地刷新，避免玩家频繁拆装升级或切换物品、流体、能量配置时反复创建容器和排序。
- Forge 1.20 的标准能力以及化学品、魔力和源质等桥接能力，在确认相邻方块不提供能力后会记录负能力结果，避免退避结束后重复执行能力查询。NeoForge 的物品、流体和能量依赖平台 `BlockCapabilityCache` 的空结果缓存与失效通知，不再重复维护负能力状态。
- 负能力只描述“能力是否存在”，不缓存容器内容、剩余容量或可接受物品，因此容器库存的高频变化不会重建缓存，也不会导致错误跳过插入模拟。
- 负能力每隔 `20 tick` 只比较目标方块状态与方块实体身份；目标被替换、区块重新加载或平台能力缓存发出失效通知时，会清除结果并恢复探测。
- 已发现的化学品、魔力和源质 handler 每次使用都会校验目标方块实体身份，并每隔 `20 tick` 对同一方块实体重新解析一次动态能力；稳定期间不再逐次调用第三方能力发现接口。
- 化学品 handler 的周期重新解析不会再清空同一方块实体上的热罐、空罐退避和拒绝记录；只有方块实体或能力确实变化时才重新学习。
- 端点的热槽、空槽退避和目标拒绝数组不会在线路建图时预分配；只有该端点首次实际学习到成功槽位、空槽或拒绝结果时，才按对应资源类型创建缓存。
- 物品库、流体库、超越维度、AE2 和 RS 的长数量端点适配器按端点弱引用复用；相邻方块实体被替换时自动换新，服务器停止时统一清理，稳定传输不再逐次创建适配器包装对象。
- 目标端点的红石、退避和拒绝缓存命中同样计入线路/服务器工作预算，但不占用 `endpointTargetAttempts` 的真实插入尝试额度。
- 目标物品槽冷扫描按剩余工作预算分段执行；未完整扫描容器时不会错误记录“整个容器拒绝”，下一次从游标位置继续。
- 启用槽数限制时，源/目标容器的限制检查同样受本轮工作预算约束；预算内无法完成严格检查时，本轮停止而不写入失败退避，避免超大容器单次完整扫描造成尖峰。
- 某种物品、流体或化学品成功传输只重置该资源的失败退避，不再清空其他资源键的拒绝记录。
- 同一线路内仅修改、加载或卸载一个普通端点时，只替换该坐标的端点缓存并增量维护资源列表和优先级；线路归属或管道拓扑变化仍使用整线重建。

## 目标插入游标

- 多槽普通物品容器使用目标插入游标，默认数量为 `9`。对于有 `N > 1` 个槽位的容器，实际最多创建 `min(9, N)` 个游标。单槽目标不创建游标，直接模拟插入槽位 `0`。
- 游标按物品类型分流，但不匹配 NBT / data components；不会保存物品堆副本。
- 第一次把某槽填满后，该槽先进入试探热槽；下一次若仍能插入，就转为稳定复用。这能覆盖“源每 5 tick 补货、目标每 5 tick 消耗、每次传输后填满”的往复场景。
- 若试探热槽已满或拒绝插入，游标转为顺序填充；槽位填满后向后移动。连续成功若干次后会再次试探热槽，以适配消费模式改变。
- 目标槽数动态变化时会按新槽数重新建立轻量游标数组，不会沿用越界槽位。

## 天穹项链运行时缓存

- 有效的物品白名单会按玩家缓存编译结果；仅当过滤器物品或其 NBT / data components 变化时重新编译。
- 周期统计直接复用提取/输入计数表、活动玩家集合和线路明细列表；稳定玩家的运行状态与展示明细同样复用，仅在维度、位置、模式、优先级或皮肤信息变化时重建明细。
- 项链槽位计数、扁平槽位定位和目标移动结果使用原始整数编码传递，不再为每次扫描创建短生命周期 record 包装对象。
- 玩家不再佩戴有效项链、过滤器失效、离线或服务器状态清理时，会同时回收白名单缓存以及提取、目标和插入游标，避免历史玩家数据常驻。
- 项链读取线路物品输入/输出端点时复用线路级只读视图，不再为每名玩家、每个工作周期复制完整端点列表。
- 客户端离开服务器时清空线路名称缓存，避免跨服务器残留和长期会话中的无效条目累积。

## 天穹物品库与流体库终端同步

- 首次打开终端时发送完整槽位快照；后续同步只发送自该菜单上次同步以来发生变化的槽位。
- 服务端每个库只保存定长的槽位变更版本数组，每个菜单只保存一个已同步版本号，不缓存完整历史快照或序列化数据。
- 同一槽位在同步间隔内连续变化多次时只发送最终状态；物品/流体总量和已用类型数随差量包一并更新。
- 流体类型删除会使后续动态槽位前移，因此从删除位置到原尾槽都会标记为变化，并显式同步尾槽清空。

## 顺序保证

**隐式固定槽位不保证插入顺序。** 热槽和游标是性能提示，不是槽位路由契约。同一物品、不同 NBT 的物品以及发生碰撞的物品类型可能复用同一游标 lane；容器内容变化后，插入位置也可能改变。

依赖严格槽位顺序、配方槽语义或固定轮转顺序的装置，应使用显式过滤/槽位限制；也可以把 `targetItemInsertionCursorCount` 设为 `0`，关闭目标游标并从槽位 `0` 开始扫描。关闭游标仍不能替代目标容器自身的插入规则。

## 相关服务端配置

- `targetItemInsertionCursorCount = 9`：目标物品插入游标上限，范围 `0..64`；`0` 表示关闭。
- `rejectedAcceptCacheSize = 9`：每个端点保存的物品、流体和化学品拒绝记录数，范围 `1..64`。
- `endpointTargetAttempts = 1`：每个源端点单轮最多尝试的目标数；默认 `1` 以控制高扇出网络成本。

超大容量的 `long` 数量存储端点不进行普通槽位扫描，因此目标插入游标对其不生效，也没有必要生效。
